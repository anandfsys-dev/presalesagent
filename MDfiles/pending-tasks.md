# Pending Tasks

- [ ] Clean up Generate CSV code — remove `handleGenerateCSV`, the `onGenerateCSV` prop from `PipelineEditor`, and any related dead code now that the button has been removed from the UI.
